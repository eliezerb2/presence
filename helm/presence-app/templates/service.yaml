apiVersion: v1
kind: Service
metadata:
  name: {{ include "presence-app.fullname" . }}
  labels:
    {{- include "presence-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presence-app.selectorLabels" . | nindent 4 }}