apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "presence-app.fullname" . }}-test-connection"
  labels:
    {{- include "presence-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "presence-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
